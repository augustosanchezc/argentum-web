# Plantilla de variables de entorno para desarrollo local.
# Copialo a .env.local y ajustá lo que necesites. .env.local esta en .gitignore.

# PostgreSQL (Docker Compose)
POSTGRES_DB=argentum
POSTGRES_USER=argentum
POSTGRES_PASSWORD=argentum_dev
POSTGRES_PORT=5432

# Redis (Docker Compose)
REDIS_PORT=6379

# Server
SERVER_PORT=3000
JWT_SECRET=cambialo_en_produccion_pero_alcanza_para_dev
NODE_ENV=development
